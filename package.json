{
  "name": "douyin-intercept-agent",
  "version": "4.0.0",
  "private": true,
  "description": "抖音截流与自动回复 Agent：Linux 授权积分服务 + Windows 可分发桌面端",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check:server": "npm --prefix server run build",
    "test:server": "npm --prefix server test",
    "check:desktop": "npm --prefix desktop run check",
    "test:desktop": "npm --prefix desktop test",
    "verify:integration": "npm run check:server && node scripts/verify-integration.mjs",
    "build:desktop": "npm --prefix desktop run build",
    "test": "npm run test:server && npm run test:desktop",
    "check": "npm run check:server && npm run check:desktop",
    "build": "npm run check && npm run build:desktop"
  }
}
