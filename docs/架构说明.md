# 抖音截流工作台 · 目标架构（重构方案 v1.0）

> 状态：草案，待甲方确认 R-1~R-5 后定稿
> 依据：`docs/需求规格.md` v1.0 + 代码考古报告（`docs/需求规格.md` §七之二 缺陷 D-1~D-14）
> 决策前提（甲方已定）：**完全重写** / **要稳定** / **node:sqlite** / **一客户端管多账号**

---

## 〇、重写与稳定的矛盾，以及解法

**矛盾**：完全重写会丢弃原作者的抖音 DOM 知识（可见 comment-list 判定、`scrollIntoView` 后延迟读坐标、note 帖浮层、Enter 三段式按键、`status_code:0` 判据……）。这些是**真账号真页面反复试错**换来的，重写无法凭空复现，且每次试错都消耗真实账号风险。

**解法：旧代码降级为"活参考 + 测试基线"，不删除。**

1. 旧代码整体移入 `legacy/`，**只读、不引用、不构建**，但保留在仓库里作为行为规格（behavior spec）。
2. 新实现的每一个 DOM 交互原语，**必须**在 `legacy/` 中找到对应实现并逐条对照（PR 里贴对照行号）。
3. 建**离线回归测试**：录下真实页面 HTML 快照存 `test/fixtures/*.html`，断言选择器仍能命中、坐标读取表达式仍能返回有效值。**不需要登录、不需要联网、不碰真账号**，可随时跑。
4. 选择器全部集中在一张表，附 `lastVerifiedAt`（最后人工验证日期）与 `confidence`（置信度）。抖音改版时先跑测试，定位失效自动标红。

> 这条是"稳定"从口号变成可验证指标的关键。

---

## 一、目标的"稳定"定义（可验收）

甲方要求"CDP 层要稳定"，但"稳定"必须落成数字，否则无法判断是否达成：

| 指标 | 定义 | 目标 |
|---|---|---|
| S-1 长稳 | 连续运行 72 小时不崩溃 | 无未捕获异常退出 |
| S-2 自愈 | 单点故障（标签页被关、Chrome 重启、网络抖动）后自动恢复 | 自动恢复率 ≥ 95%，无需人工 |
| S-3 可观测 | 任何失败都能定位到具体阶段与原因 | 每个任务有阶段化日志与失败归因码 |
| S-4 改版韧性 | 抖音改版后修复所需改动文件数 | **1 个**（选择器注册表） |
| S-5 幂等 | 进程重启、重复上报、崩溃恢复不产生重复发送或重复扣费 | 零重复 |
| S-6 隔离 | 一个账号实例故障不影响其他实例 | 故障不跨实例传播 |

**S-4 是架构约束**：如果选择器散落在多处，这条永远达不到。这是选择器注册表存在的原因。

---

## 二、总体架构

```
╔══════════════════════════════════════════════════════════╗
║  授权中心 license-server（厂商侧，VPS 部署）               ║
║                                                          ║
║  api/        登录 · 会话 · 心跳 · 上报 · 卡密兑换          ║
║  domain/     auth · billing(账本) · stats(归集) · plan    ║
║  admin/      开号 · 充值 · 看板 · 套餐                     ║
║  store/      node:sqlite（唯一写入者 + 真事务 + WAL）      ║
║  crypto/     scrypt 密码哈希 · HMAC 响应签名               ║
╚══════════════════════════════════════════════════════════╝
              ▲ HTTPS  · 签名心跳 / 幂等上报 / 卡密
              │
╔══════════════════════════════════════════════════════════╗
║  客户端 client（商家本机，一进程管多实例）                  ║
║                                                          ║
║  ┌─ host（主进程）────────────────────────────────────┐  ║
║  │  HTTP API(127.0.0.1) · 静态 UI · 调度器 · 单写者    │  ║
║  │  实例注册表 · 授权客户端(心跳/上报/停机)             │  ║
║  └───────────────────────────────────────────────────┘  ║
║         │ 进程内事件总线 / IPC                            ║
║  ┌──────┴──────┬──────────────┬──────────────────────┐  ║
║  │ browser-host│ task-runner  │ platform             │  ║
║  │ ★独占CDP WS  │ 任务状态机    │ 选择器注册表(唯一)    │  ║
║  │ 每实例一条   │ 重试/退避     │ 页面模型与交互原语    │  ║
║  └─────────────┴──────────────┴──────────────────────┘  ║
║         │                                                ║
║  ┌──────┴────────────────────────────────────────────┐  ║
║  │ adapters/  collect · comment · danmaku · dm          │  ║
║  │ 薄适配器：只做"把平台原语编排成业务动作"             │  ║
║  └───────────────────────────────────────────────────┘  ║
║                                                          ║
║  instances/<账号ID>/   ← 数据 + Chrome profile 隔离       ║
╚══════════════════════════════════════════════════════════╝
              │ CDP (9222 + N)
              ▼
        Chrome × 每实例一个独立配置目录
```

---

## 三、客户端内部：一次采集/回复任务的完整路径

以"评论区命中关键词 → 自动回复"为例，展示分层职责与**失败归因点**：

```
① 调度器  scheduler
   从实例队列取任务 → 检查积分态/急停/频控/日上限
   ↓ 拒绝则记 reason: quota_exceeded | emergency_stop | credit_exhausted
② 适配器  adapters/comment
   组装"打开视频 → 展开评论区 → 找目标评论 → 激活编辑器 → 发送"
   ↓ 每步产出 StepResult{ok, stage, reason}
③ 平台层  platform
   把步骤翻译为选择器 + 交互原语（点击/滚动/输入/按键）
   ↓ 选择器未命中 → reason: selector_miss:<key>  ← S-4 的关键
④ 核心层  core/browser-host
   独占 WS，发 CDP 命令，处理超时/重连/标签页重建
   ↓ 连接断开 → 自动重建，任务回到 queued 而非 failed
⑤ 判定    platform/publish-verifier
   嗅探 comment/publish 响应体，status_code:0 才算成功
   ↓ 空响应 → 风控拒绝，熔断并暂停该实例
⑥ 结算    写本地任务状态（原子）→ 记本地统计 → 标记待上报
```

**每一层失败都带 `stage` 与 `reason`**（S-3），UI 能直接显示"卡在哪一步、为什么"。

---

## 四、多实例隔离模型（甲方选定的形态）

一客户端管多抖音号，隔离维度：

| 资源 | 隔离方式 |
|---|---|
| Chrome 配置 | `instances/<id>/chrome-profile/`，每实例独立 `--user-data-dir` 与 `--remote-debugging-port` |
| 数据 | `instances/<id>/*.json`（队列、历史、统计、运行态） |
| CDP 连接 | 每实例一条独立 WS，由一个 `browser-host` 实例持有 |
| 调度 | 每实例独立频控/日上限/熔断窗口 |
| 授权 | 实例归属商家账号，共用该商家的积分池 |
| 故障 | 单实例异常**不传播**（S-6）：`browser-host` 按实例隔离，崩溃只重建该实例 |

**端口分配**：基线 9222，实例 N 用 `9222 + N`（可配），启动前探测占用。

---

## 五、存储设计

### 服务端（node:sqlite）

```sql
-- 商家账号
CREATE TABLE account(id, username UNIQUE, pass_hash, salt, status,
                     plan_id, created_at, expires_at, max_sessions);
-- 积分余额（单一真相）
CREATE TABLE credit(id, account_id UNIQUE, balance, updated_at);
-- 流水台账（只增不改）
CREATE TABLE ledger(id, account_id, type, amount, balance_after,
                    ref, operator, created_at);   -- type: recharge|consume|grant|refund
-- 会话
CREATE TABLE session(token_hash, account_id, device_id, issued_at,
                     last_seen_at, expires_at, revoked);
-- 套餐
CREATE TABLE plan(id, name, hours, credits, price, is_default);
-- 充值码
CREATE TABLE redeem_code(code UNIQUE, plan_id, credits, used_by,
                         used_at, batch, created_at);
-- 用量上报（幂等）
CREATE TABLE usage_report(report_id UNIQUE, account_id, instance_id,
                          window_start, window_end, online_seconds,
                          sent_ok, sent_fail, leads, payload_json, created_at);
-- 日聚合（看板用）
CREATE TABLE stats_daily(account_id, day, instance_id, source,
                         leads, sent_ok, sent_fail, credit_used,
                         PRIMARY KEY(account_id, day, instance_id, source));
```

**关键约束**：扣费与流水写入必须在**同一事务**内；`usage_report.report_id` 唯一索引保证补报幂等（S-5）。

### 客户端（JSON，单写者）

保留 JSON 但**不再有多写者**：

- 只有 `host` 主进程读写实例文件；`browser-host` 与 adapters 通过 IPC 请求，不直接碰盘。
- 写入走 `store.js`：内存态 + 原子写（`.tmp-<pid>` → rename）+ 串行化队列。
- 文件头带 `schemaVersion`，为后续迁移留路。

> 这直接消灭 D-7（队列丢任务）、`live_config.json` 三写者、评论库写坏等**全部**竞态类缺陷——因为它们的根因都是"多进程无锁覆写同一个文件"，而新架构下不存在多写者。

---

## 六、进程模型对比

| 维度 | 现状 | 目标 |
|---|---|---|
| 进程数 | 8 个各自为政，无 IPC | host + N×browser-host，全 IPC |
| 子进程句柄 | scan 丢失句柄**无法停止**（D-10） | 全部由调度器持有，可取消 |
| 重复点击 | pipeline 静默杀掉前一次（D-13） | 幂等：已在跑则拒绝并提示 |
| 顶层异常 | `process.exit(1)` 无人重启（S-1） | 捕获 → 阶段化归因 → 任务回退 → 继续 |
| 进度卡死 | 强杀后 `running:true` 永不复位（D-11） | 内存态 + 心跳超时自动归位 |
| 控制台暴露 | `0.0.0.0` + 零鉴权 + CORS `*`（D-6） | `127.0.0.1` + 会话令牌 + CORS 白名单 |

---

## 七、积分协议（必须在写代码前定稿）

| 项 | 方案 |
|---|---|
| 计费触发 | "自动回复引擎处于运行态"的挂钟时长 |
| 空闲处理 | 计入，但**空闲上限**（连续 N 小时零命中 → 停扣并标记 `idle`） |
| 计费粒度 | 服务端按心跳累计，**分段计费**（满 36 分钟计 0.5，满 60 分钟计 1） |
| 心跳频率 | 60 秒 |
| 时钟可信 | **单调时钟 + 递增序号 + HMAC 签名**；客户端时间仅作参考 |
| 断网 | 宽限期（默认 24h）内继续运行但**暂停扣费**，恢复后一次性结算 |
| 上报幂等 | `reportId` 唯一，离线囤 `pending_reports.json`，恢复后补报 |
| 停机 | 余额 ≤ 0 → 服务端拒绝心跳续期 → 客户端 **1 分钟内**进入暂停态 |
| 防篡改 | 余额、流水、授权全部服务端权威；改本机任何文件无效 |

---

## 八、目录结构

```
douyin-intercept-workbench/
├── docs/                      需求规格 · 架构说明 · 开发规范 · 部署指南
├── shared/                    ★ 双端共享契约（唯一真相）
│   ├── protocol.md            接口契约
│   ├── protocol.js            字段常量 · 校验 · 版本
│   └── errors.js              错误码与归因码
├── license-server/            授权中心（零依赖，node:sqlite）
│   ├── server.js
│   ├── api/  domain/  admin/  store/  crypto/
│   └── test/
├── client/
│   ├── 启动.cmd / 停止.cmd
│   ├── host/                  主进程：API · 调度 · 单写者
│   ├── core/                  CDP 客户端 · browser-host(独占WS) · IPC
│   ├── platform/              ★ 选择器注册表 · 页面模型 · 交互原语
│   ├── adapters/              collect · comment · danmaku · dm
│   ├── license/               心跳 · 上报 · 停机
│   ├── ui/                    前端（登录 · 看板 · 队列 · 配置）
│   └── test/
├── legacy/                    ★ 原包只读存档（行为规格参考）
└── test/
    └── fixtures/              ★ 真实页面 HTML 快照（离线回归）
```

---

## 九、分阶段计划

| 阶段 | 内容 | 产出 | 可验证 |
|---|---|---|---|
| **P0** | 移 `legacy/`，建离线 fixtures 与选择器注册表骨架 | 行为规格基线 | 测试能跑 |
| **P1** | `shared/protocol` 定稿 + `license-server`（登录/积分/心跳）+ 客户端登录 | 能登录看余额 | 验收 1-3 |
| **P2** | `core/browser-host` + `platform` 原语 + 评论区适配器（自动回复） | 能自动发评论 | 验收 4-6 |
| **P3** | 弹幕 + 私信适配器 | 三来源齐 | 验收 7 |
| **P4** | 双端看板 + 上报补报 | 数据可见 | 验收 7 |
| **P5** | 卡密 + 管理后台 + 套餐 + 多实例隔离 | 能开号收钱 | 验收 2 |
| **P6** | UI 精修（快捷键/暗色）+ 文档重写 + 72h 长稳测试 | 可交付 | S-1~S-6 |

---

## 十、已确认决策（R-1~R-5 定稿）

| # | 事项 | 结论 | 架构影响 |
|---|---|---|---|
| R-1 | 客户端 npm 依赖 | **允许极少量成熟依赖** | CDP 层改用 `ws`（见 §十一），不再自研 WebSocket；HTTP 仍用内置 `http`；存储服务端用内置 `node:sqlite` |
| R-2 | 联调条件 | **有测试账号 + 可用网络，可真机联调** | P2/P3 可交付真实验证过的适配器；离线 fixtures 仍建，作为改版回归防线 |
| R-3 | 服务端部署 | **先本地开发**，部署后置 | P1 在本机 `127.0.0.1` 跑通；HTTPS/域名/证书推迟到部署阶段 |
| R-4 | 套餐定价 | 后置 | `plan` 表先给默认占位（半年套餐 = 4320 积分），数值可后台改 |
| R-5 | Node 版本 | **接受 ≥ 22.5** | 服务端用 `node:sqlite`；客户端启动自检 Node 版本并给出明确提示 |
| R-9 | 实现语言 | **先 Node 跑通 P2 真机验证，再决定是否换 Python** | 见 §十三 语言决策记录 |

---

## 十二、语言决策记录（R-9）

### 背景

甲方提出"既然主要是自动化，能不能用 Python，Python 的自动化库更好用"。经讨论后决定：**先用 Node 跑通 P2 真机验证，用事实验证后再决定是否换语言。**

### 为什么不当场换成 Python

| 论点 | 说明 |
|---|---|
| 难点不在语言层 | 项目真正的自动化难点是对抖音 DOM 的逆向知识（挑可见的 comment-list、scrollIntoView 后延迟读坐标、note 帖浮层、Enter 三段式按键、`comment/publish` 空响应判风控）。**换语言一行都省不掉**，这些是页面知识而非语言能力 |
| 登录态死穴 | `playwright`/`selenium` 默认启动全新浏览器上下文，**用不上商家 Chrome 里已扫码登录的抖音账号**；在自动化浏览器里重新登录恰是风控最敏感的特征。原作者改用裸 CDP 驱动商家**专用 Chrome**（`--user-data-dir=dy-main`）正是为了让登录态真实可用。这是方案可行性的前提 |
| 指纹问题与语言无关 | Playwright/Puppeteer 在 Python 与 Node 下使用**同一个浏览器驱动**，`navigator.webdriver` 等指纹特征完全一致。换语言不解决指纹问题 |
| 若必选 Python | 唯一值得选的是 **DrissionPage**（国内开发，专为"接管已打开的 Chrome"设计，基于 CDP）。但它同样无法消除 DOM 脆弱性，且会推翻服务端 `node:sqlite` 方案 |
| Python 的真正优势 | **自动剪辑**（ffmpeg 封装、moviepy、whisper、OCR）。这块 Python 生态确实优于 Node，且正是需求 5 的方向——但它是**独立服务**，与 CDP 驱动层无关 |

### P2 真机验证门（Gate）

P2 完成后必须用**真实测试账号**回答以下问题，用结果决定语言走向：

| 问题 | 通过标准 | 不通过的含义 |
|---|---|---|
| G-1 登录态可用性 | 裸 CDP 接管商家专用 Chrome，评论区互动全程无需重新登录 | 若必须重新登录 → 方案本身有风险，换语言也无效 |
| G-2 自动回复成功率 | 连续 20 次自动回复，成功判定准确（不误报、不谎报） | 定位逻辑需重做 |
| G-3 风控表现 | 无验证码拦截、无账号异常提示 | 需下调频控参数或重新评估方案 |
| G-4 DOM 知识可迁移性 | 现有 legacy 中的定位技巧在新实现中全部复现成功 | 若复现困难 → 说明知识高度依赖具体实现，**更不该换语言重来** |
| G-5 稳定性 | 连续运行 8 小时无崩溃，可自愈标签页关闭 | 需加强 core 层 |

**换 Python 的触发条件**：仅当 G-1/G-2 因**语言能力**受限而无法达成时（预期不会发生，因为裸 CDP 在两种语言下等价）。若 G-1~G-5 全部通过，则维持 Node，Python 留给后期的自动剪辑服务。

### 若将来换 Python 的成本预估

| 工作项 | 影响 |
|---|---|
| `platform/` 选择器注册表 | **可平移**（纯数据，语言无关） |
| `test/fixtures/` 离线回归测试 | **可平移**（HTML 快照与断言逻辑语言无关） |
| `core/` CDP 层 | 重写（Node `ws` → Python `websocket-client`/DrissionPage） |
| `adapters/` 业务编排 | 重写 |
| `license-server/` 存储 | 方案重写（`node:sqlite` → Python `sqlite3`），但**表结构可复用** |
| `shared/protocol.md` | **完全不受影响**（契约是语言中立的） |

> 关键：**接口契约与选择器注册表是语言中立的资产**。这就是为什么 P1 先定契约——它保证语言决策可以推迟而不阻塞进度。

---

## 十一、依赖策略（R-1 定稿）

**核心原则：依赖越少越稳定，但 WebSocket 不在此列。**

| 用途 | 方案 | 理由 |
|---|---|---|
| WebSocket 客户端 | **`ws`** | Chrome CDP 必需。自研 WS 要处理分帧、掩码、背压、ping/pong、重连、半开连接——这是"要求稳定"最不该自己写的一块 |
| SQLite（服务端） | **`node:sqlite`（内置）** | 已实测可用，零依赖且有真事务 |
| HTTP 服务 | **内置 `http`** | 够用，无必要引框架 |
| 密码哈希 | **内置 `crypto.scrypt`** | 够用 |
| HMAC | **内置 `crypto.createHmac`** | 够用 |
| 前端 | **原生 DOM，无框架** | 保持单文件交付形态，符合"按键简洁高级"且无构建步骤 |

**明确不引入**：`puppeteer-core` / `playwright`。理由：现有 DOM 定位表达式是围绕裸 CDP 写的，换到 puppeteer 的 `page.evaluate` 模型要重写全部交互原语，等于把最值钱的部分推倒；且引入它会带来浏览器版本绑定与更大的分发包。

**分发影响**：允许 `ws` 意味着客户端首次运行需要 `npm install` 或随包内置 `node_modules`。**推荐随包内置**（把 `node_modules/ws` 一起打包），这样商家仍是"解压即用"，不需要装 npm 环境。启动脚本加一句依赖自检：缺失时给出明确提示而非崩溃。

---

## 十三、待确认事项

原 R-1~R-5 已全部定稿（见 §十）。新增：

| # | 事项 | 影响 |
|---|---|---|
| R-6 | 授权中心与客户端是否同仓库（当前是单仓库 `license-server/` + `client/`） | 决定 CI 与发布流程 |
| R-7 | 商家机器上是否要求安装 Node，还是打包成 exe | 决定分发方式（当前假设需要 Node ≥ 22.5） |
| R-8 | 半年套餐的具体积分与定价 | 决定 `plan` 表初始数据 |

---

*本文档为重构方案。文档体系（需求规格 · 架构说明 · 开发规范 · 部署指南 · 接口契约）完成后进入 P0。*
