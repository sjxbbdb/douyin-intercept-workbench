# AGENTS.md — 项目红线与工程约定

> **本文件是 AI 编码智能体与人类开发者的第一入口。**
> 在写任何代码之前，读完**第 1 节（红线）**与**第 2 节（必须避免的误实现）**。
>
> 完整文档索引见 `README-DEV.md`。技术事实源见 `shared/术语与选型基准.md`。

---

## 0. 项目一句话

「抖音自动回复工作台」：帮助商家自动回复抖音视频评论、直播间弹幕、私信，并把意向客户沉淀为线索。
客户端（商家 Windows）+ 授权中心（厂商 Linux 服务端）两段式，通过 Chrome 远程调试协议（CDP）驱动商家本机**已登录的专用 Chrome**。按**成功回复条数**计费。

**技术栈**：Node.js ≥ 22.5 / 纯 JavaScript（CommonJS，无 TypeScript、无构建）/ 唯一第三方依赖 `ws` / 服务端 `node:sqlite` / 客户端 JSON 文件 / 前端原生 DOM。

---

## 1. 🔴 三条红线（不可违背）

> 这三条不是建议，是产品存在的前提。**违反任何一条的实现都必须重做。**

### 红线 1：安全上限由服务端下发，客户端只能调低

**背景**：这个工具的价值来自"发得多"，而账号被封的原因也是"发得多"。二者根本对立。因此产品定位是**「控制节奏，让账号活得长」**。

- 权威数值在 `shared/protocol.md` §4.6 的 `tier_table`——**全系统唯一来源**
- **禁止在任何地方硬编码**限额、间隔、阈值。必须由 `tier_table` 实时推导
- 客户端**只能更保守**：`daily_max` 只能调低、`min_interval_ms` 只能调高且在区间内、`content_similarity_max` 只能调低、`active_hours` 只能调短
- 越权上报 → 服务端返回 `POLICY_VIOLATION(409)` 并拒绝
- **观察期（第 1–3 天）日上限为 0**（`sending_enabled=false` / `collect_only=true`），只采集不发送，**客户端不可跳过**

### 红线 2：只对平台确认成功的发送计费

计费依据**仅有** `POST /api/v1/audit/sends` 的逐条明细，且必须**同时**满足：

```
verdict = sent_confirmed
且 confirm_signal = platform_response     ← DOM 判断不算，必须拿到平台响应体
且 platform_status_code = 0
且 未超当日策略上限
且 该 send_id 未计费过
```

失败、被风控拒绝、被跳过、超上限的发送**一律不计费**。

- ⚠️ **空响应 = 风控拒绝**，不得标记成功
- ⚠️ `send_id` 必须在**发送前**生成并落盘（幂等键 + 计费键）
- ⚠️ 绝不允许"DOM 上看起来发出去了"就判定成功

### 红线 3：审计必须记录真实生效的策略值

- 每次发送与配置变更都要留痕，审计须含**实际生效的策略版本与生效值**，而不只是用户设置值
- 原因：纠纷时须能回答"该账号当时实际生效的策略是什么、用户是否主动调高过、系统是否拒绝过"。**只记设置值无法自证**
- **隐私边界**：只上传哈希与计数，**绝不上传**评论原文、回复内容原文、`sec_uid` 原文、用户隐私字段
- 审计本地与服务端**双写**，客户端不可篡改服务端副本

---

## 2. ⚠️ 必须避免的误实现（本项目已踩过的坑）

> 每条给出【错误做法】【为什么错】【正确做法】。**写代码前逐条对照。**

### 2.1 把 DOM 现象当作发送成功

- ❌ **错误**：编辑器消失 / 评论出现在列表里 → 判定发送成功
- ⚠️ **为什么错**：旧代码就这么写的，结果是**把页面崩溃记为"已发送"**，统计虚高、审计失真、客户投诉
- ✅ **正确**：用 CDP `Network` 域嗅探平台接口响应体，`status_code === 0` 才算成功；DOM 只用于"继续下一步"
- 🔍 **自检**：搜索代码里判定成功的位置，确认依据是响应体而非 DOM

### 2.2 硬编码安全数值

- ❌ **错误**：`const DAILY_MAX = 30`；`const MIN_INTERVAL = 60000`
- ⚠️ **为什么错**：平台风控变化时无法统一调整；且客户端可被破解绕过
- ✅ **正确**：全部从服务端下发的 `policy` 读取，且套餐积分、额度文案都由 `tier_table` 实时推导
- 🔍 **自检**：全局搜索日上限/间隔/相似度阈值的字面量，应只出现在测试与契约文档中

### 2.3 允许客户端调高上限

- ❌ **错误**：客户端上报 `daily_max: 200`，服务端照单全收
- ✅ **正确**：服务端逐项比对策略，更激进即返回 `POLICY_VIOLATION(409)`，并记录该次越权尝试供举证
- 🔍 **自检**：构造一个超过策略的上报，确认被拒绝且有审计记录

### 2.4 内容相似度方向写反

- ❌ **错误**：相似度**低于**阈值才拒绝
- ✅ **正确**：相似度**超过**阈值即拒绝发送（`0.85` = 超过 85% 拒绝）
- 🔍 **自检**：用两条几乎相同的文案测试，第二条应被拒绝

### 2.5 对失败的发送也计费

- ❌ **错误**：只要尝试发送就扣积分
- ✅ **正确**：只有平台确认成功才计费；失败、风控拒绝、跳过、超上限一律不计费
- 🔍 **自检**：制造一次被拒绝的发送，确认余额未变且台账无该笔记录

### 2.6 客户端说成功就计费

- ❌ **错误**：信任客户端上报的 `verdict`
- ✅ **正确**：服务端按 `send_id` 幂等 + 校验 `confirm_signal=platform_response` + `platform_status_code=0`，不满足则不计费
- 🔍 **自检**：伪造一条 `confirm_signal: "dom_stable"` 的上报，确认不计费

### 2.7 忘记在发送前生成 `send_id`

- ❌ **错误**：发送成功后再生成 ID
- ⚠️ **为什么错**：崩溃或断网后无法幂等 → **重复发送、重复计费**
- ✅ **正确**：发送**前**生成并落盘，补报时复用
- 🔍 **自检**：发送中途杀进程，重启后确认不会重复发送同一条

### 2.8 空 catch 吞异常

- ❌ **错误**：`try { ... } catch {}`
- ⚠️ **为什么错**：旧代码的去重历史写盘失败被静默吞掉，导致**同一评论被重复回复**且无人发现
- ✅ **正确**：至少记录日志 + 归因码；影响数据一致性的失败必须上报
- 🔍 **自检**：搜索 `catch {}` 与 `catch (e) {}`，应为 0 处

### 2.9 多进程各自读写同一份数据

- ❌ **错误**：server 与 worker 都直接 `writeFileSync` 同一个 JSON
- ⚠️ **为什么错**：旧代码因此静默丢任务、配置回退
- ✅ **正确**：**单写者**（只有 host 主进程碰盘）+ **原子写**（`.tmp-<pid>` → rename）；其他模块经 IPC
- 🔍 **自检**：搜索客户端代码里的 `writeFileSync`，应只出现在 `host/store.js`

### 2.10 忽略"页面同时存在隐藏与可见两套 DOM"

- ❌ **错误**：`document.querySelector('[data-e2e="comment-list"]')` 直接取第一个
- ⚠️ **为什么错**：隐藏的那套里按钮尺寸为 0，操作必然失败
- ✅ **正确**：筛选**可见**元素（非零尺寸 + 在视口内 + `display!=none` + `visibility!=hidden`）
- 🔍 **自检**：在真机上验证选择器返回的是可见容器

### 2.11 用固定间隔发送

- ❌ **错误**：每 10 秒发一条
- ⚠️ **为什么错**：固定节奏是最容易被识别的机器人特征
- ✅ **正确**：对数正态随机化间隔 + 活跃时段限制（默认 08:00–23:00）+ 操作延迟随机化
- 🔍 **自检**：观察日志里的实际间隔分布，不应集中在某个固定值

### 2.12 搞混"工作台账号"与"抖音账号"

- ❌ **错误**：把两者当成同一个东西
- ✅ **正确**：**工作台账号**由厂商下发，登录客户端界面；**抖音账号**是商家自己的，登录在专用 Chrome 里。两者完全独立
- 🔍 **自检**：见 `shared/术语与选型基准.md` §1.1

### 2.13 本地控制台对外暴露

- ❌ **错误**：本地 API 绑 `0.0.0.0`、零鉴权、CORS 用 `*`
- ⚠️ **为什么错**：旧代码就这样，导致**任意网页可跨站调用接口代替商家发出评论**，局域网内也可访问
- ✅ **正确**：绑 `127.0.0.1` + 会话令牌 + CORS 白名单
- 🔍 **自检**：`netstat` 确认监听地址是回环

### 2.14 引入白名单外的依赖

- ❌ **错误**：为了省事引入 `puppeteer`、`axios`、`express` 等
- ⚠️ **为什么错**：见 `shared/术语与选型基准.md` §3.3——`puppeteer`/`playwright` 会给浏览器打自动化指纹且**用不上商家已登录的账号**，直接破坏方案可行性
- ✅ **正确**：白名单只有 `ws`。引入新依赖必须先报备
- 🔍 **自检**：`package.json` 的 dependencies 只应有 `ws`

---

## 3. 目录与文件约束

```
shared/lib/          双端共享（协议常量、stableStringify、错误码）
license-server/      授权中心（Linux）
client/host/         客户端主进程（唯一写盘者）
client/core/         CDP 基础设施（cdp.js 全项目唯一一份）
client/platform/     抖音平台知识（★ selectors.js 是选择器唯一来源）
client/adapters/     业务适配器
client/license/      与授权中心通信
client/safety/       安全护栏（独立模块，不得散落在适配器里）
client/ui/           前端
legacy/              旧代码只读存档（禁止被生产代码 require）
test/fixtures/       真实页面 HTML 快照（离线回归）
```

**硬约束**：

- ⚠️ `license-server/` 与 `client/` **不得互相 require**，只能通过 `shared/protocol.md` 定义的 HTTP 接口通信
- ⚠️ **选择器只能定义在 `client/platform/selectors.js`**。其他文件出现 `data-e2e` 等选择器字符串视为缺陷
- ⚠️ `legacy/` **只读**，不得被生产代码引用
- ⚠️ 路径必须经 `REPLY_WORKSPACE` 或 `path.join` 解析，**禁止写死绝对路径**（旧代码因此在新机器上跑不通）

---

## 4. 常用命令

```bash
# 语法检查（每次改动后至少执行）
node --check client/host/main.js
node --check license-server/server.js

# 自测（项目统一入口）
node test/run.js

# 确认 node:sqlite 可用
node -e "const s=require('node:sqlite');console.log(Object.keys(s))"

# 客户端启动（Windows）
client\启动.cmd

# 服务端健康检查
curl -sS http://127.0.0.1:18080/healthz
```

---

## 5. 必读文档（按角色）

| 你要做什么 | 先读 |
|---|---|
| 任何开发 | 本文件 §1 §2 → `README-DEV.md` → 对应方案的任务清单 |
| 写 CDP 代码 | `shared/已知陷阱与平台知识.md`（**必读**） |
| 实现服务端 | `shared/protocol.md`（全部）+ `docs/部署指南-服务端.md` |
| 实现计费/审计 | `docs/需求规格.md` FR-3、FR-6 + `shared/protocol.md` §4.8 §6 |
| 实现安全护栏 | `docs/需求规格.md` FR-2.3 + `shared/安全与合规要求.md` |
| 写测试 | `shared/测试策略.md` |
| 作为 AI 智能体开发 | `shared/AI协作开发指引.md`（**专为智能体编写**） |

---

## 6. 完成定义（Definition of Done）

一个功能算"完成"，必须同时满足：

- [ ] `node --check` 通过
- [ ] `node test/run.js` 全绿
- [ ] 若涉及页面操作：**真机验证过**（不是"代码写完"就算完成）
- [ ] 不违反三条红线
- [ ] 本文件 §2 的 14 条误实现逐条自查通过
- [ ] 涉及限额/计费的，有对应测试用例
- [ ] 新引入的选择器已加入 `platform/selectors.js` 且带"最后验证日期"

> ⚠️ **"代码写完"不等于"完成"。** 涉及抖音页面操作的功能，未在真实账号上验证过，不得声明完成。汇报时请明确区分"已真机验证"与"仅代码完成"。

---

## 7. 做什么之前必须先问人

遇到以下情况**停下来问**，不要自行决定：

- 需要放宽、绕过任何安全限制（红线 1）
- 需要引入白名单外的依赖
- 需要改变计费口径或计费资格判据（红线 2）
- 需要修改审计字段或隐私边界（红线 3）
- 需要改动 `tier_table` 的任何数值
- 需要真实抖音账号才能验证，但手头没有
- 发现文档之间互相矛盾

---

## 8. 修订记录

| 日期 | 变更 |
|---|---|
| 2026-09-18 | **全文重写**。作废旧版"回复分发必须保留人工确认，不要把队列改成无确认批量自动发送"条款（与项目需求自动回复正面冲突）。确立三条红线、14 条误实现清单、完成定义。 |
