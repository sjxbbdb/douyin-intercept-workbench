# 工作日志 · 原子链路（2026-09-24）

> 目的：记录环境、问题、修复与验证结果，使后续接入 sidecar 的人能直接复用结论。
> 全部内容已脱敏：不含 sec_uid、昵称、头像、会话原文、Cookie 或 token。

---

## 1. 环境

| 项 | 值 |
|---|---|
| 操作系统 | Windows 11 专业版（10.0.22631） |
| Node.js | v22.23.2（零第三方依赖） |
| Chrome | 153.0.8010.53，CDP 端口 9222，商家手动扫码登录 |
| 本地工作台 | 127.0.0.1:8090（仅查看/入队，链路不依赖） |
| git | 2.55.0.windows.5 |

---

## 2. 时间线（问题 → 定位 → 修复 → 验证）

| # | 问题现象 | 定位到的真正原因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 视频关键词命中 0 个 | 标题几乎不含完整连续串 | 分词 + 顺序无关匹配 | 0 → 多条命中 |
| 2 | 评论只加载 5 条 | 仅改 scrollTop 触发不了加载 | 滚轮事件 + scrollTop + 点展开回复 | 单视频百条级 |
| 3 | 评论区看似存在但按钮 0×0 | 隐藏容器 | 判断可见性（宽度>0） | 按钮可点 |
| 4 | 评论找到了、按钮找不到 | 滚动触发虚拟列表回收 | 定位+点击合并为一次求值（原子） | 连续 5 条成功 |
| 5 | 带表情评论永远匹配不上 | [流泪] 两侧处理不一致 | 页面侧同样整段删 [xxx] | 匹配恢复 |
| 6 | 昵称永远匹配不上 | 零宽字符 / 截断 | 昵称只作优先项 | 匹配恢复 |
| 7 | 接口有、页面没有 | 评论网页版不渲染 | 如实报 comment_not_rendered，不重试 | 不再刷失败 |
| 8 | 同一评论回两次 | 入队缺硬校验 | cid 在队列或历史里一律不再入队 | 二次入队被拒 |
| 9 | 私信文案填进顶部搜索框 | 取了「全页第一个可见 input」 | 只在 im-dialog 内查找输入框 | 文案进入私信框 |
| 10 | 私信面板宽度卡 0–1px | 标签页不可见 / 查到隐藏面板 | 唤醒窗口 + 取可见面板 | 连续多条发送成功 |
| 11 | 私密账号发不出去 | 平台账号级限制 | 打开主页阶段识别 → 直接跳过 | 同批 2 条被正确跳过 |
| 12 | 回车看似没发出 | 零宽字符让「已清空」判定失败 | 校验前剔除 U+200B 等 | 判定恢复准确 |
| 13 | 评论回复连续失败 | 节奏过快触发静默限流 | 间隔 45–80 秒、每批 3–5 条 | 同批 5 条全部成功 |
| 14 | 回复到无关评论 | 关键词按单字命中（噪音极大） | 新增 phrase/segment 模式，入队前断言 | 1165 → 9 条命中，0 条不符 |

---

## 3. 验证证据（脱敏截图）

| 图 | 内容 |
|---|---|
| 01-atomic-chain-overview.png | 五步链路与关键实现点、定位三原则、文本匹配三坑 |
| 02-reply-worker-evidence.png | 评论回复成功路径日志 + 失败分类 + 节奏实测 |
| 03-dm-worker-evidence.png | 私信发送与私密账号跳过日志 + 双重校验依据 + 面板三因 |
| 04-environment.png | 当前环境、组件与数据产物、安全边界 |
| 05-problems-and-fixes.png | 14 条问题→原因→修复对照 |

生成方式：`board.html` 渲染为页面后经 CDP `Page.captureScreenshot` 截取（2 倍分辨率），内容全部为脱敏文本，不含任何页面截图与个人信息。

---

## 4. 尚未解决 / 下一步

1. **每日配额**未实现（建议 REPLY_DAILY_LIMIT，到量自动停）；
2. **窗口可见性**：标签页被完全遮挡时私信面板仍可能不展开；彻底解法需独立 Chrome 实例（disable-backgrounding-occluded-windows、disable-renderer-backgrounding），未验证；
3. **接入 sidecar**：参考实现未与 `probe/*.py` 合并，需按 PR-BODY §4 的步骤另行提交；
4. **企业号 / 官方 API**：未验证。

---

## 5. 脱敏声明

- 证据图中所有用户名、昵称一律以「用户A/B/C」或占位符替代；
- 不含 sec_uid、cid、Cookie、token、二维码、会话原文；
- 参考实现保留代码逻辑，但仓库内不包含任何运行数据文件。
