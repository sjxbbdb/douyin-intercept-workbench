feat(probe): 直播间流程补上关键词匹配（九步流程第 2 步），成批前过滤

对齐重新确认过的九步流程：监听 → 按关键词匹配 → 去重并形成批次 → 话术选择（平台侧）→
公屏回复 → 确认成功才私信 → 私信话术（平台侧）→ 私信 → 保存检查点。
原实现缺第 2 步（全收再成批），本次补齐：

- live_flow：新增 filtered 状态与 normalize_filter / match_event / filter_is_active；
  take_batch 支持 filters，先按窗口过期、再按关键词匹配、最后成批；
  匹配语义直接复用 crawl 的匹配器（phrase/seg/all/any），排除词优先，与评论链路一致；
  响应 filter 给出 matched / missed / excluded 计数。
- sidecar：live_plan 接受 keywords / excludeKeywords / matchMode；
  未命中或队列为空时不建立批次（延续上一轮空批次修复的行为）。
- find_event 顺带返回解析后的 detail / private / payload，便于调用方读取状态。

回归测试（离线）：新增 3 个用例覆盖 命中才成批 / 排除词优先 / 无关键词时行为不变；
LiveFlowTests + BoundaryTests 共 29 项通过。

仍未完成并如实标注：积分 / 功能开关 / 服务端审计未接入；真机验收（选择器、作者标识、
公屏送达、私信送达）仍未进行；live_batch.autoEligible 保持 false。